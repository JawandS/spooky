[project]
name = "spooky-halloween"
version = "1.0.0"
description = "A spooky Halloween-themed website with Flask and Tailwind CSS"
requires-python = ">=3.11"
dependencies = [
    "Flask==3.0.0",
    "gunicorn==21.2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
